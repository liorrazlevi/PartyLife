<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="24dp"
    android:layout_marginBottom="32dp"
    android:layoutDirection="rtl"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground">

    <!-- תמונת המסיבה -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cvImageContainer"
        android:layout_width="match_parent"
        android:layout_height="180dp"
        app:cardCornerRadius="24dp"
        app:cardElevation="4dp"
        app:cardBackgroundColor="#2D1B2E"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:id="@+id/ivEventCover"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            tools:src="@tools:sample/backgrounds/scenic" />

    </androidx.cardview.widget.CardView>

    <!-- שם המסיבה - תופס את השורה עד הכפתור -->
    <TextView
        android:id="@+id/tvEventTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="8dp"
        android:text="שם המסיבה"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:textStyle="bold"
        android:gravity="start"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/ivDeleteParty"
        app:layout_constraintTop_toBottomOf="@id/cvImageContainer" />

    <!-- כפתור מחיקה - מוגדל ונוח ללחיצה -->
    <ImageView
        android:id="@+id/ivDeleteParty"
        android:layout_width="45dp"
        android:layout_height="45dp"
        android:padding="8dp"
        android:src="@android:drawable/ic_menu_delete"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:visibility="gone"
        tools:visibility="visible"
        app:tint="#FF5252"
        app:layout_constraintTop_toTopOf="@id/tvEventTitle"
        app:layout_constraintBottom_toBottomOf="@id/tvEventTitle"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- תאריך ושעה -->
    <TextView
        android:id="@+id/tvEventDate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="תאריך ושעה"
        android:textColor="#999999"
        android:textSize="14sp"
        android:gravity="start"
        app:layout_constraintTop_toBottomOf="@id/tvEventTitle" />

    <!-- מיקום -->
    <TextView
        android:id="@+id/tvEventStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="מיקום"
        android:textColor="#9575CD"
        android:textSize="14sp"
        android:textStyle="bold"
        android:gravity="start"
        app:layout_constraintTop_toBottomOf="@id/tvEventDate" />

</androidx.constraintlayout.widget.ConstraintLayout>
